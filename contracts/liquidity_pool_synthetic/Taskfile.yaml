version: '3'
tasks:
  clean:
    cmd: cargo clean

  fmt:
    cmd: cargo fmt --all


  check:
    cmd: cargo check

  build:
    cmds:
      - task -d ../config_storage build || exit 1
      - task -d ../rewards_gauge build || exit 1
      - task -d ../token_pool build || exit 1
      - task -d ../liquidity_pool_plane build || exit 1
      - soroban contract build --package liquidity_pool_synthetic
      - soroban contract optimize --wasm ../../target/wasm32v1-none/release/liquidity_pool_synthetic.wasm --wasm-out ../../target/wasm32v1-none/release/liquidity_pool_synthetic.wasm
      - cp ../../target/wasm32v1-none/release/liquidity_pool_synthetic.wasm ../../wasm/liquidity_pool_synthetic.wasm

  test:
    deps:
      - build
    cmd: cargo test

  test_slow:
    deps:
      - build
    cmd: cargo test --features=slow_tests

  default:
    cmds:
      - task: build

  all:
    cmds:
      - task: clean
      - task: fmt
      - task: build
      - task: check
      - task: test
